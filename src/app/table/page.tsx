import { PageShell } from "@/components/page-shell";

export default function TablePage() {
  return (
    <PageShell
      title="League Table"
      description="Standings, points, form and goal difference will be populated from the football data pipeline."
    />
  );
}
