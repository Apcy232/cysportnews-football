import { PageShell } from "@/components/page-shell";

export default function ResultsPage() {
  return (
    <PageShell
      title="Results"
      description="Completed matches and final scores will appear here after automatic result imports are active."
    />
  );
}
