import { PageShell } from "@/components/page-shell";

export default function FixturesPage() {
  return (
    <PageShell
      title="Fixtures"
      description="Upcoming Cyprus First Division matches will appear here after the API-Football import is connected."
    />
  );
}
