import { PageShell } from "@/components/page-shell";

export default function PlayersPage() {
  return (
    <PageShell
      title="Players"
      description="Player profiles and season statistics will appear here once squad imports are connected."
    />
  );
}
