import { PageShell } from "@/components/page-shell";

export default function NewsPage() {
  return (
    <PageShell
      title="News"
      description="Editorial and curated league news will live here after the content model is added."
    />
  );
}
